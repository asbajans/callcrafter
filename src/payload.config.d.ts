// Type declaration for payload.config
// This file provides type information for the configuration module
// without creating a module that conflicts with the actual implementation

declare namespace config {
  // Empty namespace for type checking purposes
}

// Re-export the actual config as a type
export type ConfigType = unknown;

export default ConfigType;
